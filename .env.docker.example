# Puerto publicado en Windows y puerto utilizado por la API.
HOST_PORT=8080
PORT=8080

# Dentro de la red de Docker, mongo funciona como nombre del servidor.
MONGODB_URI=mongodb://mongo:27017/shipnow

NODE_ENV=production
LOG_LEVEL=info
